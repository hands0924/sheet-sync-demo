name: Deploy Cloud Function

on:
  push:
    branches:
      - main
  workflow_dispatch:  # 수동 실행 옵션 추가

jobs:
  deploy:
    name: Deploy sheet_webhook Function
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Set up gcloud CLI
        uses: google-github-actions/setup-gcloud@v1
        with:
          project_id: ${{ secrets.GCP_PROJECT }}
          # 서비스 계정 키를 JSON 문자열로 환경변수에 저장
          service_account_key: ${{ secrets.GCP_SA_KEY }}
          export_default_credentials: true

      - name: Install dependencies (for local lint/test, optional)
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Deploy Cloud Function
        run: |
          # 환경 변수로 함수 배포 시 설정
          gcloud functions deploy sheet_webhook \
            --gen2 \
            --runtime python310 \
            --region=us-central1 \
            --entry-point=sheet_webhook \
            --trigger-http \
            --allow-unauthenticated \
            --memory=256MB \
            --timeout=60s \
            --min-instances=0 \
            --max-instances=10 \
            --set-env-vars="SHEET_ID=${{ secrets.SHEET_ID }},FIRESTORE_DOC=${{ secrets.FIRESTORE_DOC }},SOLAPI_API_KEY=${{ secrets.SOLAPI_API_KEY }},SOLAPI_API_SECRET=${{ secrets.SOLAPI_API_SECRET }},SENDER_PHONE=${{ secrets.SENDER_PHONE }}"

      - name: Verify Deployment
        run: |
          gcloud functions describe sheet_webhook \
            --region=us-central1 \
            --format="value(serviceConfig.uri)"
